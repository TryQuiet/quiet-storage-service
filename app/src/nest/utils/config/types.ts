export enum Environment {
  Development = 'development',
  Production = 'production',
  Local = 'local',
  Test = 'test',
}

export enum EnvironmentShort {
  Dev = 'dev',
  Prod = 'prod',
  Local = 'local',
  Test = 'test',
}
