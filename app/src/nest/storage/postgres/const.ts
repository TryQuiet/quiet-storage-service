export enum TableNames {
  COMMUNITIES = 'communities',
  LOG_ENTRY_SYNC = 'log_entry_sync',
  LOG_ENTRY_SYNC_COUNTER = 'log_entry_sync_counter',
}

export enum PostgresSources {
  LOCAL = 'local',
  RDS = 'rds',
}
