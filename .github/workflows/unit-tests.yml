name: Unit Tests

on:
  pull_request:

jobs:
  unit-tests:
    runs-on: ubuntu-24.04

    steps:
      - name: "Setup environment"
        uses: ./.github/actions/setup-env

      - name: "Run unit tests"
        shell: bash
        run: pnpm run test:ci
